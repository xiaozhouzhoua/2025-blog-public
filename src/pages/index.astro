---
// ...existing code...
---

<Layout>
  // ...existing code...

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {sortedPosts.map((post) => (
      <PostCard post={post} />
    ))}
  </div>

  // ...existing code...
</Layout>

